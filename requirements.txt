# Core Data Processing
pandas>=1.3.0
numpy>=1.24.0
openpyxl>=3.0.9

# SFTP / FTP
paramiko>=2.7.2

# GUI (Tkinter is built-in with Python, but these are extras)
tkcalendar>=1.6.1

# Web Dashboard (Streamlit)
streamlit>=1.25.0
plotly>=5.14.0

# Scheduling
schedule>=1.1.0

# Excel Chart Generation (if needed)
xlwings>=0.30.0

# Email (optional - for SMTP)
# These are built-in: smtplib, email

# Additional utilities
python-dotenv>=1.0.0  # For environment variables (SMTP passwords)

# For PDF generation (optional)
# reportlab>=4.0.0
# fpdf2>=2.7.0

# Word report (copy-paste ready .docx, with embedded trend charts)
python-docx>=1.1.0
matplotlib>=3.8.0